name: Kotlin Spring Boot CI/CD

on:
  push:
    branches: [ '**' ]
  pull_request:
    types: [ opened, synchronize, reopened, closed ]
    branches: [ main, master ]

jobs:
  lint-and-test:
    if: |
      (github.event_name == 'pull_request' && github.event.action != 'closed') ||
      (github.event_name == 'push' && github.ref != 'refs/heads/main' && github.ref != 'refs/heads/master')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run ktlint
        run: ./gradlew ktlintCheck

      - name: Set up Docker for Testcontainers
        run: |
          echo "TESTCONTAINERS_HOST_OVERRIDE=localhost" >> $GITHUB_ENV
          docker ps

      - name: Run unit tests (excluding E2E)
        run: ./gradlew test --tests "com.intezya.abysscore.*Test" --exclude "*E2ETest"
        continue-on-error: true

      - name: Run e2e tests separately
        run: |
          ./gradlew test --tests "com.intezya.abysscore.*E2ETest" --info
        continue-on-error: true

      - name: Save test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: test-reports
          path: |
            build/reports/tests/
            build/test-results/
            **/build/reports/tests/
            **/TEST-*.xml
          retention-days: 7

  build-and-push:
    if: |
      (github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true) ||
      (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'))
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up Docker for Testcontainers
        if: github.event_name == 'push'
        run: |
          echo "TESTCONTAINERS_HOST_OVERRIDE=localhost" >> $GITHUB_ENV
          docker ps

      - name: Run ktlint and unit tests for main/master push
        if: github.event_name == 'push'
        run: |
          ./gradlew ktlintCheck
          ./gradlew test --tests "com.intezya.abysscore.*Test" --exclude "*E2ETest"
        continue-on-error: true

      - name: Run e2e tests separately for main/master
        if: github.event_name == 'push'
        run: |
          ./gradlew test --tests "com.intezya.abysscore.*E2ETest" --info
        continue-on-error: true

      - name: Save test reports for main/master
        if: github.event_name == 'push' && always()
        uses: actions/upload-artifact@v3
        with:
          name: test-reports-main
          path: |
            build/reports/tests/
            build/test-results/
            **/build/reports/tests/
            **/TEST-*.xml
          retention-days: 7

      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') || github.ref == format('refs/heads/{0}', 'master') }}
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=pr

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
